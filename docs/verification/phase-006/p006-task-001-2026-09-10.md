# Phase 006 validation packet

Date: 2026-09-10
Phase: `CORE-PHASE-006`
Task: `P006-TASK-001` through the currently executable portions of `P006-TASK-005`
Status: Open. The final phase is not complete.

## Candidate identity

The candidate is built from commit `7e3a7707d6add2c920cc39f552a41168a6ffb148` on branch `envy/phase-006-final-validation`. The metadata version was changed to `2.4.1` in commit `5bc4499`, and the hybrid shop buy and sell GameTest was added in commit `7e3a770`.

| Field | Value |
| --- | --- |
| Candidate | `build/libs/futureshops-2.4.1.jar` |
| Size | `1366027` bytes |
| SHA-256 | `e000fa5f46de4b7e8682717f0900a0ea51ce798a3c70c367461f3541c60d52e4` |
| SHA-512 | `73f9e953dbedabc8210789918503abe637c824fa3c40f84e78f393bd2e58c86be2cf3f2c7766e308fd1445442f923af097b1f73b6b31aa0f88c95ec5f5e7ceee` |
| Minecraft | `1.21.1` |
| NeoForge | `21.1.248` |
| Java | `21` |
| Goal SHA-256 | `4fb3c6ba8004f4841b23b9b7111543c258f3d43bfb3deeec076d68be081fcb1e` |

`unzip -tq build/libs/futureshops-2.4.1.jar` reported no archive errors. A second `jar` build produced the same SHA-256. The manifest declares version `2.4.1`, loader range `[4,)`, Minecraft range `[1.21.1,1.22)`, and NeoForge range `[21,)`.

The prior 2.4.0 identity remains distinct and unchanged: source `ec43c71e4a9c433b222189862b2dda8bdd115137`, candidate SHA-256 `f28de22b0ab56b8a38eb5ec538394b5a6df26c128253748a46680a5cd95563fd`. Its original validation record is historical evidence only.

## Packaging and isolation

The candidate archive contains only `com/enviouse/futureshopsp` production classes and FutureShops resources. It contains no nested jars or copied Pixelmon, DanConomy, Vault, FinalEconomy, EverNifeCore, Bukkit, bridge, SQLite, or proof artifacts. Optional integrations remain runtime name and descriptor based. The exact Pixelmon input used for the native runtime was not modified and has SHA-256 `9020393f98382ae8794ef2694e7bec1984c1a0eca735ea3eea06e0cb151c61f2`.

## Deterministic checks

The following commands passed on `node-1` with Java 21.

```text
JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 bash ./gradlew test --tests com.enviouse.futureshopsp.server.economy.BoundEconomyOperationCoordinatorTest --tests com.enviouse.futureshopsp.server.economy.EconomyJournalSavedDataTest --tests com.enviouse.futureshopsp.server.economy.EconomyReceiptAuditJournalTest --no-daemon
JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 bash ./gradlew test --no-daemon
JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 bash ./gradlew build --no-daemon
```

The focused suite, complete unit suite, and production build all passed. No formatter or lint task exists in this repository.

## Dedicated GameTest checks

A fresh server-only runtime without optional mods used `eula=true` and passed all `33 required tests`. Its sanitized log SHA-256 is `30a8d3cb98e5ca9c7b6b8bc39c0fddb348f560e556e913efe864d9c75ec98a20`. This confirms the current candidate's internal and common server paths.

A second fresh server-only runtime was run with `-PverificationGameDirectory=/tmp/futureshops-p006-gametest-current-gjBCwh`, read back `eula=true`, reached `Done`, logged `FutureShops server starting`, passed all `33 required tests`, and logged `FutureShops server stopping`. Its log SHA-256 is `bf89d7d2f79840971549aced371a7a9d88741b0b90f4c3698a1b8fdfaf3ed321`. This independently revalidates the current candidate's common server path after the Phase 006 documentation commit.

A fresh server-only runtime with the exact Pixelmon 9.4.0 jar used `eula=true`. The log recorded:

```text
FutureShops Pixelmon mixin target com.pixelmonmod.pixelmon.api.storage.PlayerPartyStorage apply true
All 33 required tests passed :)
```

The Pixelmon log SHA-256 is `91913240bd9c2183089b0bdfbc1c146040f41e7085e4c85553c90dc48fdc0558`. This proves that the native Pixelmon target applies and that the existing native GameTest matrix passes. The conditional hybrid shop test is skipped when no hybrid account service is present, so this run is not exact hybrid shop proof.

The new `pixelmonHybridShopBuyAndSellSuccess` test drives `ShopBuyService.handleBuyRequest` and `ShopSellService.handleSellRequest` through the coordinator. When an exact hybrid account is available, it seeds `1000`, buys a diamond for `500`, sells an iron ingot for `25`, asserts the resulting balance `525`, and asserts custody is clear. It is present in the candidate source but has not been executed against the exact external hybrid stack in this phase.

An exact hybrid server runtime was assembled at `/tmp/futureshops-p006-exact-hybrid-fresh5-C2IrwS` from Youer build `889`, commit `d4a204a0d00e95b11b4caae000ccaa79171e4d9a`, with server SHA-256 `47ff03d9c26e40eac38ff5bbc1108f170d4b1649dfcc74488b696546f5807006`, Pixelmon 9.4.0 SHA-256 `9020393f98382ae8794ef2694e7bec1984c1a0eca735ea3eea06e0cb151c61f2`, PixelmonEconomyBridge 1.1.6 SHA-256 `409896ee42f4163b616c5ab0964c220fc0a1c910ce8c3e2a0c05c4d78bd21da6`, FinalEconomy 1.0.9 SHA-256 `4cc7ba1aab02fffd86d2aa009a51ac4e6ca8590776ce9a13c8a2f45fdf01f529`, EverNifeCore 2.0.4.4 SHA-256 `15585a223a76c7bf18b311aa3e07db71ef4a1969837608a9db1d27e99c52f6e3`, Vault 1.7.3 SHA-256 `a6b5ed97f43a5cf5bbaf00a7c8cd23c5afc9bd003f849875af8b36e6cf77d01d`, and the exact receipt bridge fixture SHA-256 `cd4547e192c2b059efdf515b26ec0787680954ecc038765165aecbe2d30f364a`. The runtime read back `eula=true`, loaded all six plugins, reached `Done`, and applied the Pixelmon mixin. The positive run used Java 21 and bound to `127.0.0.1:25575`.

The disposable probe waited for the delayed PixelmonEconomyBridge registration, used the bounded `FEBankAccount` test account, and invoked the actual server `ShopBuyService.execute` and `ShopSellService.execute` handlers. The public packet wrappers were not used because a headless fake player cannot receive their clientbound payloads. The exact runtime log recorded:

```text
futureshops.p006.hybrid_probe lifecycle=READY provider=pixelmon diagnostic=
futureshops.p006.hybrid_probe status=CONFIRMED seed=true seed_error=OK buy=true buy_error=OK sell=true sell_error=OK buy_balance=600 sell_balance=625 final_balance=625 diamonds=1 iron=0
```

The bridge trace recorded durable commits for the seed deposit from `100` to `1100`, the buy withdrawal from `1100` to `600`, and the sell deposit from `600` to `625`. The Java 21 exact runtime log recorded `JVM identified ... Java 21.0.11`, `FutureShops Pixelmon mixin target com.pixelmonmod.pixelmon.api.storage.PlayerPartyStorage apply true`, and `Done`. Its decisive log line was `futureshops.p006.hybrid_probe status=CONFIRMED seed=true seed_error=OK buy=true buy_error=OK sell=true sell_error=OK buy_balance=600 sell_balance=625 final_balance=625 diamonds=1 iron=0`. The exact runtime log SHA-256 is `200292013f2557b3a9e15f6c743c2911f666dded92b9cf20837565eaeea3099d`. This is fresh exact hybrid server evidence for both shop directions on Java 21. It does not replace the required connected client gate or the dedicated GameTestServer route, which is separately limited by the Youer constructor failure documented below.

## Dedicated server smoke

The first smoke attempt on default port `25565` refused to bind because an unrelated preexisting server already owns that port. No unrelated process was stopped. Two isolated retries bound to `127.0.0.1:25567` and `127.0.0.1:25568`, reached the Minecraft `Done` line, logged `FutureShops server starting`, and logged clean FutureShops shutdown. Their log SHA-256 values were `ffa54859136db185927c457da2de4c38cf00f6d8d365f3ff3d13456167d04819` and `b707f993625689c6820e70286a50d4f95f0ab8d7d2c8239b3fed30b2885f7819`. Gradle returned exit `143` because the owned Java process was stopped directly after startup. This is startup evidence, not a full successful Gradle smoke task.

A final isolated retry on `127.0.0.1:25569` also reached `Done` and shut down cleanly. Its log SHA-256 is `2d9bbc8b41554ee552162c2e1e03f52c69fbfc2ab41f480ee97718c5d4065102`. It likewise ended with Gradle exit `143` after controlled process termination.

## Standard laptop client smoke

An isolated PrismLauncher instance named `futureshops-p006-client-20260910` was created on the authorized laptop `envision`. Its copied `minecraft` directory contained only the candidate and GeckoLib. The candidate SHA-256 was `e000fa5f46de4b7e8682717f0900a0ea51ce798a3c70c367461f3541c60d52e4` and the GeckoLib SHA-256 was `a1b6ce25e8627aa7e748672eedb6b71af68e0993462313649c259f38e42bcac9`. The isolated instance had `soundCategory_master:0.0` before launch.

PrismLauncher started the client with Microsoft OpenJDK `21.0.7`. The owned window was `Minecraft NeoForge 1.21.1`, the renderer reported `NVIDIA GeForce RTX 5090 Laptop GPU` and OpenGL `4.6`, and the client log listed FutureShops `2.4.1`, GeckoLib `4.8.4`, Minecraft `1.21.1`, and NeoForge `21.1.248`. FutureShops common setup completed and the sound engine started. The recreated Java playback stream was matched to the owned process and reported `Volume: 1.00 [MUTED]` before acceptance work. The sanitized capture SHA-256 is `02996176d53735630dfaea5ecf6f031f6c8a17cbefb6d248cfdc37ce59cf74b1` and the client log SHA-256 is `29fe07061fd45f4d97f93016e25cbf3efdc671f2fa59446d324abb8b274dd5ab`.

The isolated client and its launcher process were stopped, the owned Java stream disappeared, and the exact test instance and launcher log were removed. This completes the standard no-optional-plugin laptop startup and audio boundary check. It does not prove the exact hybrid server connection, localized interaction, stale snapshot recovery, or reconnect path.

## Matching snapshot, migration, and restore

A complete matching snapshot was created from disposable runtime `/tmp/futureshops-p006-restore-6Wmc7A` after a clean exact-stack startup and stop. It included the complete `world`, `config`, `mods`, `plugins`, `libraries`, `youer-config`, `youer.jar`, `server.properties`, and `eula.txt` trees. The snapshot contained `305` files. The snapshot manifest digest was `203f6d63a1a042f84df65166228aa9fda4da6692bbe00797d549eb3d2d7c446d` for SHA-256 and `1ee27ad3439c61d8b453faf307d93f95b07ee3162b42b77ff3f8fe3344afa2986e4383451d17f117581b251b4fa1fc68a4b24fd3b16e74b194ae59e5cf08b6e6` for SHA-512. The supplied Pixelmon jar remained unchanged at SHA-256 `9020393f98382ae8794ef2694e7bec1984c1a0eca735ea3eea06e0cb151c61f2`.

The full restore copy passed the pre-restore SHA-256 manifest check, read back `eula=true`, started Youer `1.21.1-d4a204a0`, NeoForge `21.1.248`, FutureShops, Pixelmon, PixelmonEconomyBridge, FinalEconomy, EverNifeCore, Vault, and GeckoLib, reached `Done (21.062s)!`, and shut down cleanly. The restored provider image SHA-256 was `90d7d14795311cade82c4c6d3a80b9cc40e774e2bf7e8f4e3537ed6eeaac5e0e`; the FutureShops journal SHA-256 was `24f22d035222263c15cd7c35c262d811238d6b9cbd0fbf556bffca4524fea388`; and the clean receipt marker SHA-256 was `a84083b47c3fdff796a6097cf809ee3db82f79b141e4b3cf74a0ae94187b7d77`. The restore log SHA-256 is `56b27099a7298e17a5274f6b5c45794b76b845bd67cd2fffd741d492b9f69156`. This is copy-based matching backup and restore evidence, not a schema conversion.

Recovery behavior was checked with exact copies. Appending to only `server.properties` caused the manifest check to fail with one mismatched file and no restore was accepted. An interrupted migration copy containing only `world` and `config` also failed the manifest check. Replacing that incomplete copy with the complete snapshot and rerunning the manifest produced `resumed_manifest_status=OK` for all `305` files. This proves that recovery preserves one complete matching lineage and refuses partial or damaged copies.

## Remaining phase gates

The exact `FEBankAccount` shop buy and sell path now has fresh Java 21 server evidence above. A fresh restart readback was then run from the same stopped runtime with the final receipt id supplied to the fixture so the external account image was retained. The restarted Java 21 server reached `Done`, the bridge retained the bounded account receipt, and the probe recorded `futureshops.p006.hybrid_recovery lifecycle=READY provider=pixelmon diagnostic= balance=625 result_status=CONFIRMED`. The restart log SHA-256 is `54aa823eb063e12f24f8f750a82e92c62b65527adc9639fbcd44a985cc6d1a2d`. This proves a clean stop and fresh-process account and receipt readback.

A separate exact hybrid crash runtime at `/tmp/futureshops-p006-exact-hybrid-crash-fva1DK` injected `before_receipt_persist` and exited with code `90`. The crash log SHA-256 is `6e008106840e08ce339a866257bcf426946bc21549ea7eb005d648c7bc836a3d`. Before restart, the world marker was `unclean` and the durable receipt directory contained `PREPARED` and `EXTERNAL_PENDING` records. Restarting the same runtime produced `futureshops.p006.hybrid_recovery lifecycle=FROZEN provider=pixelmon diagnostic=economy persistence integrity requires operator recovery balance=-1 result_status=RECOVERY_REQUIRED`. The recovery log SHA-256 is `2da0f0ab88ee471bb4e77051f54a2ac09eed6450772dd90712253ceb9bcd135f`. The Pixelmon `npcs.dat` end of file message on this abrupt halt is an expected fixture artifact and is not interpreted as a FutureShops transaction result. This proves the unknown external outcome is frozen instead of guessed or replayed.

A fresh exact hybrid runtime at `/tmp/futureshops-p006-rebuild-gG0tVM` was then used for the remaining provider-side durability checks. The provider registration waited for the delayed PixelmonEconomyBridge setup and bound the exact `br.com.finalcraft.pixelmoneconomybridge.compat.v1_21_R1.reforged.finaleconomy.FEBankAccount` wrapper. A withdrawal request `00000000-0000-0000-0000-000000000101` returned `CONFIRMED` for balance `100`, precheck, mutation from `100` to `75`, receipt lookup, and `save-all flush`. The decisive log SHA-256 is `ece565bc5dd1f169bc0ede0e268b6bdf8e723bfbcdbec871e60fbf99a435f6e1`.

The scheduled-save race used request `00000000-0000-0000-0000-000000000104`, dispatched the provider mutation from an async caller while seven main-thread `save-all flush` operations ran, and then performed an independent lookup and balance read. The account remained at `75`, the mutation and lookup were both `CONFIRMED`, and the receipt remained present. The race log SHA-256 is `dcda1d1b93508cf96145a89e272e93e34fb2727a38828aefcaa8c169a4d3bfe8`.

An injected `after_external_mutation` crash for request `00000000-0000-0000-0000-000000000102` exited with code `90` after the bridge persisted the external receipt and before process shutdown. Its crash log SHA-256 is `94a556d44ce008dce743cf259d6833f4e03ff2de3aad86d6abedcf9a7d56b930`. A fresh restart with mutation disabled performed only provider balance and receipt lookup, returned account class `FEBankAccount`, balance `75`, and `CONFIRMED` lookup for the same request. Its restart log SHA-256 is `3a0e8be9088a36b2942e540e14a70f9eede4abf810ac8bca1f2b4cd65cab3cbe`. This is independent post-crash external receipt lookup proof.

An injected `before_receipt_persist` crash for request `00000000-0000-0000-0000-000000000103` also exited with code `90` before the receipt image was written. Its crash log SHA-256 is `139194a9ecec72e8da2e1bde19cea3a9adc288b93a285ecb08a7b18c60d7d1f8`. A fresh restart with mutation disabled returned account class `FEBankAccount`, balance `100`, and `RECEIPT_NOT_FOUND` for that request, with no request `103` entry in the persisted account image. Its restart log SHA-256 is `c993991fa18fd379c77515587699273ddb21e2ec819b008c9129a5410d3a98f8`. This preserves the no-receipt prior image and does not treat absence alone as proof of an accepted FutureShops transaction.

Migration and full matching restore are now complete for the copy-based recovery rehearsal above. Historical Phase 005 probe evidence remains labeled historical and is not promoted to Phase 006 proof.

The standard laptop client gate is complete for isolated startup, renderer, common setup, and mute verification. The exact hybrid client then connected to the same candidate server at `100.76.164.109:25577` as `FSProbe006` after a clean server restart. The client log recorded the second join, accepted `/futureshops debug on all`, and opened the `default` shop. The server diagnostic session recorded the account class `custom_or_hybrid`, provider result `CONFIRMED`, and all six observed capabilities as true. The client opened the Diamond Sword detail screen, confirmed a buy, and displayed `You own: 1`. A second client action sold one sword and displayed `Sold 1 item(s) for 750.` The owner supplied the decisive sell screenshot; its SHA-256 is `b3916341f76ea8d6ab727ba3b12ecbaccc911314e828b97a3a665656cf25e65e`.

The matching server log recorded `futureshops:c2ssellrequestpacket(1)` and `futureshops:s2csellresponsepacket(2)` in the same runtime. A fresh debug capture for the sell was not active when that click occurred, so the receipt and balance proof remains the authoritative Java 21 server probe and the provider durability records above. The exact hybrid reconnect, client rendering, buy, sell, packet delivery, and stale snapshot claims are now evidenced. Server-side stale request handling is also covered by the coordinator tests.

## Snapshot revision protocol repair and connected stale request check

The client stale request gate was repaired before this run. Shop sessions now carry a monotonically increasing snapshot revision. Shop data packets, buy and sell request packets, and buy and sell response packets carry that revision and a bounded response reason. A request whose revision no longer matches the server session is rejected before provider precheck, inventory mutation, escrow, or balance mutation. The server sends a silent fresh shop snapshot after the rejection. The client ignores older snapshots and renders the localized server state change message for `STALE_REQUEST`. This is protocol version `26`.

The repair passed the focused codec, session revision, and client state tests, the complete unit suite, and the Java 21 production build. The fresh server-only GameTest run passed all `33 required tests`; its sanitized log SHA-256 is `85f852940eb264702a73862afdce56d79ccbe4ec8a0654e92a15c659106796b6`. The pre-commit candidate used for this connected check had SHA-256 `672510a54affd89a2db245cc4da29b6cdf970f118bd84b258c164164ed9fa440` and passed `unzip -tq`.

The authorized laptop client used the same connected candidate server at `100.76.164.109:25577`, with the owned Minecraft process and audio stream isolated and muted. The exact shop detail screen held a Diamond Sword buy confirmation. RCON `shopadmin reload` refreshed one active shop session at `17:15:12.610`, advancing the server snapshot while the confirmation retained the prior revision. The confirmation was then sent from the connected client. The client displayed `Operation failed due to a server-side state change. Please retry.` in the FutureShops dialog. The decisive screenshot SHA-256 is `945c82542459c63920f0028bf7c5dba063c264feef2c78631444dc3df80172a7`.

Post-action RCON checks reported `--name balance: 1000.00 Coins` and an empty inventory. No successful buy, receipt, custody, or inventory mutation was recorded. The server log records the refresh and the enabled debug session `4cc32375-e88d-4e24-b70f-e78ca0a8955b`; stale rejection intentionally returns before provider diagnostics, so no provider mutation line is expected for this path. This is the connected client stale snapshot observation that was previously open.

The owned client process, launcher, screenshots, and exact PrismLauncher instance were stopped and removed after the screenshot hash and RCON evidence were recorded. The dedicated server process and its exact temporary runtime were stopped and removed. Personal Minecraft instances and unrelated clients were preserved.

Issue 66 was not updated, no Phase 006 pull request or tag was created, and no release or platform upload was performed because support documentation and final integration gates remain open.

## Cleanup

The following exact test-owned directories were removed after their decisive log lines and hashes were recorded:

```text
/tmp/futureshops-p006-pixelmon-vIrJdI
/tmp/futureshops-p006-gametest-7d0qMw
/tmp/futureshops-p006-server-FLa00q
/tmp/futureshops-p006-server2-wL3U8L
/tmp/futureshops-p006-server3-sgXbla
/tmp/futureshops-p006-server4-qDLou1
/tmp/futureshops-p006-gametest-current-gjBCwh
/tmp/futureshops-p006-exact-hybrid-jHwRiM
/tmp/futureshops-p006-exact-hybrid-fresh3-ph5dFn
/tmp/futureshops-p006-exact-hybrid-fresh4-pDcS8P
/tmp/futureshops-p006-exact-hybrid-fresh5-C2IrwS
/tmp/futureshops-p006-exact-hybrid-crash-fva1DK
/tmp/futureshops-p006-probe
/tmp/futureshops-p006-external-xO9tIM
/tmp/futureshops-youer-src-cIRZVJ
/tmp/futureshops-p006-hybrid-client-MRdB8X
/tmp/futureshops-p006-hybrid-client-screen*.png
/tmp/futureshops-p006-hybrid-client-20260910b-clients*.json
```

The exact hybrid runtimes, probe source, external downloads, and Youer source clone were retained until this packet recorded their decisive results, then removed. The connected hybrid server runtime, client screenshots, client launcher log, client process, and isolated PrismLauncher instance were also removed after the owner supplied the sell result. The personal `1.21.1` PrismLauncher instance was preserved. No owned Minecraft process, test audio stream, or hybrid test window remains. The candidate jar under `build/libs` is retained as the requested unpublished deliverable. The preexisting `run` directory and the exact supplied Pixelmon jar were preserved.

## Result

The candidate is reproducible, loads on NeoForge 1.21.1, applies the native Pixelmon mixin, passes the current headless 33-test matrix, completes the exact Java 21 hybrid shop buy and sell path, passes provider scheduled-save and crash-boundary checks, completes matching snapshot, migration, and restore rehearsal, freezes unknown external outcomes instead of guessing or replaying them, and rejects a connected stale client request before mutation. The exact hybrid laptop client now proves reconnect, debug, shop navigation, buy, sell, stale rejection, renderer, and mute behavior. Phase 006 remains open until support documentation and the final issue, review, merge, and tag gates are run and pass.
