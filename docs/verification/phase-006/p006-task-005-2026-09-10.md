# Phase 006 support and delivery audit

Date: 2026-09-10
Phase: `CORE-PHASE-006`
Task: `P006-TASK-005`
Status: Complete for the repository-owned documentation and delivery audit. Final integration remains open.

## Delivered artifact check

The frozen candidate is `build/libs/futureshops-2.4.1.jar` with SHA-256 `c4d8aa2dc0f14d7fe3e1d7c08b1f398d1984938420cf527c03d34efa02b843ba` and SHA-512 `4aada00eaf1c2a7857260ba2411f8babfca758416a75c66a9c009224b0c800558755a3335665ee184332d61a746c87902ad6db56e35ebb648a1bab7cbb7a0096`. `unzip -tq` passed. The archive contains FutureShops classes and resources only. No nested jars or copied Pixelmon, DanConomy, Vault, Bukkit, bridge, SQLite, FinalEconomy, EverNifeCore, or proof classes were found.

The manifest reports FutureShops `2.4.1`, Minecraft `[1.21.1,1.22)`, NeoForge `[21,)`, and source commit `a7c9315bac8ba92f3254e43e5627965330f9cc6a`. The requested artifact remains unpublished in `build/libs`.

## Documentation rehearsal

The support guide was exercised against the current candidate evidence packet. The operator procedure is:

```text
/futureshops debug status
/futureshops debug on all
/futureshops debug status
/futureshops debug off
/futureshops debug status
```

The bounded capture contract was checked in `DebugDiagnosticsTest` with:

```text
JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64 bash ./gradlew test --tests com.enviouse.futureshopsp.server.debug.DebugDiagnosticsTest --no-daemon
```

The test completed with `BUILD SUCCESSFUL`. It verifies default off state, the module allowlist, ephemeral session creation, idempotent reset, command registration, optional mixin declarations, request identity fields, artifact identity, capability separation, and safe next action fields.

The exact hybrid capture in the main Phase 006 packet used the same candidate and recorded the account class, provider result, declared and observed capabilities, request identity, journal, receipt, custody, claim, stale response, and cleanup. The connected stale snapshot check additionally proved the localized state change message and no balance or inventory effect after an authoritative shop refresh. Server logs and dedicated GameTests remain the primary evidence, with the client used only for the named synchronization and visible interaction claims.

## Documentation and link audit

The root README, maintainer documentation, documentation index, Pixelmon guide, DanConomy guide, Vault proof references, backup and restore runbook, and economy debugging guide were read through their current links. The duplicate provider paragraph in the root README was removed. The index includes the Phase 006 validation packet and this audit. Relative links resolve to tracked files.

The support documentation states the exact provider and loader pins, restart only provider selection, account capability proof, bounded diagnostic limits, receipt and backup lineage, migration and frozen recovery behavior, no generic wrapper or legacy Vault acceptance, stale snapshot handling, redaction rules, and the requirement to preserve matching world and provider data. It does not claim a 3.0.0 implementation or publication.

## Security and recovery audit

The repository scan found no credential patterns, private keys, or secret-bearing additions in the changed product files. The jar contains no third party bytes. The evidence packet preserves exact hashes and sanitized observations only. No journals, receipts, custody, claims, provider data, personal instances, or unrelated processes were deleted. Test-owned runtimes, screenshots, client processes, and temporary logs were removed after their decisive evidence was recorded.

## Remaining gates

Issue 66 has not yet been updated. The Phase 006 pull request, required checks, independent review, GitHub merge into `1.21.1`, signed annotated Phase 006 tag, and final integration readback remain open. No release or platform upload is authorized by this audit.
